name: Hello World

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello from GitHub Actions!"

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Run Tests
        run: echo "Running tests..."
