name: Hello World

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello from GitHub Actions!"
  test:
      runs-on: ubuntu-latest
      steps:
        - name: Print Tests
          run: echo "Running tests..."

